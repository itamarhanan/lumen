{
  "name": "lumen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "./dev.sh",
    "lint": "turbo lint",
    "test": "turbo test",
    "check-types": "turbo check-types",
    "migrate:clickhouse": "CLICKHOUSE_DB=${CLICKHOUSE_DB:-lumen} bash -c 'envsubst < clickhouse/migrations/001_events.sql | curl -s -X POST http://localhost:8123/ --data-binary @-'",
    "ci": "pnpm install && pnpm turbo lint && pnpm turbo check-types && pnpm knip && pnpm turbo test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "eslint": "catalog:",
    "supabase": "^2.100.1",
    "turbo": "^2.9.8",
    "typescript-eslint": "catalog:",
    "knip": "^5"
  },
  "dependencies": {
    "typescript": "catalog:"
  }
}
